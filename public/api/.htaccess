# Konfigurationsdateien mit Zugangsdaten nie direkt ausliefern.
# Die Endpoints binden sie per require ein – das bleibt erlaubt.
<FilesMatch "-config\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>

# Verzeichnisauflistung abschalten
Options -Indexes
